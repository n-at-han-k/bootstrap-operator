{{- if .Values.leaderElect }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "bootstrap-operator.fullname" . }}-leader-election
  namespace: {{ include "bootstrap-operator.namespace" . }}
  labels:
    {{- include "bootstrap-operator.labels" . | nindent 4 }}
rules:
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "bootstrap-operator.fullname" . }}-leader-election
  namespace: {{ include "bootstrap-operator.namespace" . }}
  labels:
    {{- include "bootstrap-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "bootstrap-operator.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "bootstrap-operator.serviceAccountName" . }}
    namespace: {{ include "bootstrap-operator.namespace" . }}
{{- end }}
