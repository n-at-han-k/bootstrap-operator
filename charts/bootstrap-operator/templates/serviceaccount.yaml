{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bootstrap-operator.serviceAccountName" . }}
  namespace: {{ include "bootstrap-operator.namespace" . }}
  labels:
    {{- include "bootstrap-operator.labels" . | nindent 4 }}
{{- end }}
